# Allow `pnpm add` at the workspace root (used by cloudflare/wrangler-action@v3
# which auto-installs wrangler before deploying). Without this, pnpm 10 hard-
# fails with ERR_PNPM_ADDING_TO_ROOT.
ignore-workspace-root-check=true
